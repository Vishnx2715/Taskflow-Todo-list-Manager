# Taskflow-Todo-list-Manager
A browser-based task management application built with HTML, CSS, and JavaScript, offering task creation, categorization, prioritization, filtering, and search. It runs entirely on the client side using localStorage, features a responsive UI with light and dark themes, and requires no backend.
